# Operators

# Identity Operators
# is Returns True if both variables are the same object and is returns False if both variables are not same.

x = [1, 2, 3]
y = [1, 2, 3]

print(x is y)
print(id(x))
print(id(y))

print(x is not y)